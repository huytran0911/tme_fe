version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps  # Thêm flag này ở đây
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
